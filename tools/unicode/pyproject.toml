[project]
name = "unicode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cfeyen", email = "cafeyen21@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "unicodedata2==17.0.0",
]

[project.scripts]
unicode = "unicode:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
